import { AbstractComponentDto, AbstractComponentLoader, CommonNetClient, Component, SdkSettings, SimpleProgress } from '@ohos/sdkmanager-common';
export declare class UniOhRemoteComponentLoader extends AbstractComponentLoader {
    private _ohLoader;
    private _settings;
    constructor(settings: SdkSettings, netClient: CommonNetClient, progress?: SimpleProgress);
    protected getLocation(component: AbstractComponentDto, sdkRoot: string): string | undefined;
    load(): Promise<Component[]>;
}
