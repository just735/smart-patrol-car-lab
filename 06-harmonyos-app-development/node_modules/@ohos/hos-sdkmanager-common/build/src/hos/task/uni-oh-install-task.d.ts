import { AbstractInstallTask, CommonNetClient, Component, Progress, SdkSettings } from '@ohos/sdkmanager-common';
export declare class UniOhInstallTask extends AbstractInstallTask {
    constructor(component: Component, sdkSettings: SdkSettings, netClient: CommonNetClient);
    download(progress: Progress): Promise<string>;
    protected getUniPackageName(): string;
}
