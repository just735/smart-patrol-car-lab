import { AbstractLicenseManager, CommonNetClient, Component, License, Progress, SdkSettings } from '@ohos/sdkmanager-common';
export declare class UniOhLicenseManager extends AbstractLicenseManager {
    private readonly _sdkSettings;
    private readonly _netClient;
    private _components;
    constructor(sdkSettings: SdkSettings, netClient: CommonNetClient, components: Component[]);
    protected getLicensesRootLocation(): string;
    protected getReadLocalHashErrorMessage(licensesLocation: string): string;
    retrieveLicenses(licenseIds: string[], progress: Progress): Promise<Map<string, License>>;
}
