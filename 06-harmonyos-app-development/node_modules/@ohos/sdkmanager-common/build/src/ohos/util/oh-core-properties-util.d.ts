/**
 * 读取properties文件配置的工具类
 * 1、由于读取的方法有点特殊，当前文件与sdk-oh-core.properties文件的相对位置如果发生变化，就需要修改下面代码中的文件路径
 * 2、这里读取的properties文件内容的方式是将整个文本全部读出来，然后根据分隔符进行分割，由于换行符在不同操作系统有差异，
 * 因此字段与字段之间采用英文逗号分隔,增肌新的字段时要注意！！！
 * 例如：
 * key1=value1,
 * key2=value2,
 * ...
 */
export declare class OhCorePropertiesUtil {
    private static INSTANCE;
    private _contentMap;
    constructor();
    /**
     * 单例
     */
    static getInstance(): OhCorePropertiesUtil;
    /**
     * 从properties文件中获取值
     * @param key
     */
    getString(key: string): string;
}
