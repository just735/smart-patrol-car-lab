"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.OhCorePropertiesUtil = void 0;
const fs_1 = __importDefault(require("fs"));
const path_1 = __importDefault(require("path"));
const utils_1 = require("../../core/util/utils");
/**
 * 读取properties文件配置的工具类
 * 1、由于读取的方法有点特殊，当前文件与sdk-oh-core.properties文件的相对位置如果发生变化，就需要修改下面代码中的文件路径
 * 2、这里读取的properties文件内容的方式是将整个文本全部读出来，然后根据分隔符进行分割，由于换行符在不同操作系统有差异，
 * 因此字段与字段之间采用英文逗号分隔,增肌新的字段时要注意！！！
 * 例如：
 * key1=value1,
 * key2=value2,
 * ...
 */
class OhCorePropertiesUtil {
    constructor() {
        this._contentMap = new Map();
        const properties = fs_1.default.readFileSync(path_1.default.resolve(__dirname, '../../../res/sdk-oh-core.properties'), 'utf-8').split(',');
        properties.forEach(str => {
            const keyValueArray = str.trim().split('=');
            if (keyValueArray.length === 2) {
                const key = keyValueArray[0];
                const value = keyValueArray[1];
                this._contentMap.set(key, value);
            }
            else if (keyValueArray.length === 1) {
                const key = keyValueArray[0];
                this._contentMap.set(key, '');
            }
        });
    }
    /**
     * 单例
     */
    static getInstance() {
        return OhCorePropertiesUtil.INSTANCE;
    }
    /**
     * 从properties文件中获取值
     * @param key
     */
    getString(key) {
        const value = this._contentMap.get(key);
        if ((0, utils_1.isNotEmpty)(value)) {
            return value;
        }
        return '';
    }
}
exports.OhCorePropertiesUtil = OhCorePropertiesUtil;
OhCorePropertiesUtil.INSTANCE = new OhCorePropertiesUtil();
